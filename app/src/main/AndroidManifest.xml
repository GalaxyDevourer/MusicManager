<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chmnu.groupmanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.SearchActivityConstraint" />
        <activity android:name=".activities.ShowTracksActivity" />
        <activity android:name=".activities.AddSongsActivity" />
        <activity android:name=".activities.TaskActivity" />
        <activity android:name=".activities.UsersListActivity" />
        <activity android:name=".activities.TasksListActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".activities.BandsListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.AddBandsActivity"
            android:parentActivityName=".activities.BandsListActivity" />
        <activity android:name=".activities.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>